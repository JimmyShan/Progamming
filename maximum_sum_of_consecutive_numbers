#Written Oct 12,2014
#given an array of numbers, return the maximum sum of consecutive numbers. 


def consum(s):                             # s is the array of numbers
    n=len(s)
    maxsum = currentsum = 0
    
    for i in range(n):
        currentsum = currentsum + s[i]
        if currentsum > maxsum:
            maxsum = currentsum
        if currentsum < 0:
            currentsum = 0
            continue
    return maxsum
    
print consum([-1,2,-3,4,-2,3,11,3,-4,2])
